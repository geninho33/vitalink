{
  "name": "vitalink-api",
  "version": "1.0.0",
  "description": "VitaLink API — autenticação JWT, RBAC, clínico e auditoria",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:schema": "mysql -h %DB_HOST% -u %DB_USER% -p%DB_PASSWORD% < ../database/schema.sql"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.12.0"
  }
}
