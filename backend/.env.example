# Servidor
NODE_ENV=development
PORT=3333
API_PREFIX=/api/v1

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=vitalink
DB_PASSWORD=vitalink_secret
DB_NAME=vitalink

# JWT
JWT_SECRET=troque-este-segredo-em-producao-vitalink-2026
JWT_EXPIRES_IN=8h

# CORS (origem do frontend React)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:5175

# E-mail (opcional — sem SMTP o link é retornado em dev_link)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=VitaLink <noreply@vitalink.local>
APP_PUBLIC_URL=http://localhost:5173
